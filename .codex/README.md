# CAPT — Codex Agent Instructions

This repository is **workspace-native**. Do not rely on a long bootstrap prompt.

Start here:

1. Read `AGENTS.md` (repository entrypoint + authority order).
2. Read `CURRENT_STATE.md` (live state) and `CHECKPOINT.md` (resume point).
3. Inspect `TASK_QUEUE.md` / `tasks/` for active work.
4. Run `python3 capt_cli.py workspace validate` before claiming the workspace is consistent.
5. Follow the operating rules in `AGENTS.md` and the trust boundaries in `SECURITY_BOUNDARIES.md`.

The repository is the source of truth. This file is a thin pointer, not a constitution.
