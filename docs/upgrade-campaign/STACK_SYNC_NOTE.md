# Campaign Stack Synchronization

This file records that the Sol-Reconciliation upgrade stack may contain temporary branch-to-branch synchronization merges when a predecessor is repaired after dependent branches already exist.

These synchronization merges preserve both histories and do not authorize merge to `main`.
