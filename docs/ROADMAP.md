# CAPT Core Roadmap

This roadmap is status-oriented: completed integration work is separated from release authorization and future product tranches.

## Historical numbered release — v0.5

Preserved under `release_evidence/v0.5/`. Those artifacts remain historical evidence for that exact lineage.

## Protected `main`

`main` contains the established runtime/productization foundation and pinned authored-skill integration. It remains intentionally behind the terminal candidate until PR #117 satisfies its merge gates.

## Terminal convergence — implemented and verified

- [x] reconcile Discovery and hardened Ouroboros/provider lifecycle into one authority spine;
- [x] reconcile CAPT-UPG-001→019 rather than merging stale stacked bases blindly;
- [x] exact historical replay correction and governed replay fork;
- [x] durable Cohort persistence/evidence admission + governed steering + Chamber;
- [x] governed workspace mutation/promotion lifecycle;
- [x] capability lease inspection/revoke, forensic flight bundle, provenance and epistemic projections;
- [x] first-class local OpenAI-compatible execution and bounded prewarm;
- [x] native `CAPTNativeMac` chat/operator target and session hardening;
- [x] coherent provider/model persistence and retirement of the false generic native MLX placeholder;
- [x] authored-skill bytes bound into exact model-visible approval identity;
- [x] macOS ↔ RuntimeService ↔ MCP shared-ledger denial/execution/replay/restart acceptance;
- [x] normal Swift, strict concurrency, and ThreadSanitizer verification on the reconciled candidate.

## Terminal merge gates still open

- [ ] Security Closure Cockpit must have legitimate exact-head evidence for every applicable release-blocking control and return `releaseAuthorized=true`;
- [ ] build and hash the final exact wheel/sdist/native artifact after the final source/docs freeze;
- [ ] record final exact-head verification and artifact identities on PR #117;
- [ ] only then merge PR #117 to protected `main`.

The security gate is not bypassed because ordinary tests are green.

## Explicitly separate work

- [ ] CAPT-UPG-020 reciprocal-review benchmark: empirical campaign evidence pending;
- [ ] CAPT-UPG-021 sparse symbol-index probe: real-repository benchmark pending;
- [ ] CAPT-UPG-022 Tree-sitter structural-hash probe: runtime benchmark pending;
- [ ] CAPT-UPG-023 FastCDC probe: runtime/provider-cache evidence pending;
- [ ] CAPT-UPG-024 cognitive-debt cockpit: exact-head verification remains its own gate;
- [ ] Inversion Labs / Forge edition: maintain independent governed edition lineage;
- [ ] Inversion Eval: maintain independent MCP-repository lineage.

## Public-release product tranche

The owner-approved design/plan lineage for Secure Intake/Quarantine, human-first results, Projects, composer context palette, Search/Deep Research governance, and Cohort Council remains separate from terminal Core convergence. Implement it only from its approved design/plan authority.

## Later hardening/product work

- final signing/notarization/distribution/auto-update evidence for the native macOS app;
- CAPT-managed encryption for sensitive authoritative persistent state where required by the security profile;
- independently rooted/signed audit attestations;
- stronger process/container isolation for write-capable autonomous drivers;
- explicit paid-service billing caps/alerts and evidence collection;
- native MLX execution only when a real adapter exists and is independently verified;
- multi-principal isolation if/when the product threat model expands beyond one trusted local OS user.

A roadmap checkbox is not release evidence.
