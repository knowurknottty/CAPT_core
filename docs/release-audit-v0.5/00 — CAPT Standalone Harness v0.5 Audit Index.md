---
audit_date: 2026-08-05
project: CAPT Standalone Core Harness
candidate_sha: b45c4b005c9171172d055697a55034006bb0f2fe
repair_sha: b79c4f05784d001268e3fef523755365b1f5888e
release_state: CAPT_MODEL_OPERATOR_PROVEN_AND_RELEASE_READY
wheel: capt_solo-0.5.0-py3-none-any.whl
wheel_sha256: 348fe9da477e0323d9c9b294677a1e10de4f9245373a27300367a9e8bdf879b3
source_repository: /tmp/capt-final-integration
source_branch: release/capt-standalone-final
audit_type: Release Audit & Verification-Contract Repair Review
tags: [capt, audit, release, standalone-harness, v0.5]
---

# 00 — CAPT Standalone Harness v0.5 Audit Index

**Project:** CAPT Standalone Core Harness
**Audit date:** 2026-08-05
**Source repository:** `/tmp/capt-final-integration` (local worktree of `CAPT_core`)
**Source branch:** `release/capt-standalone-final`
**Candidate SHA (release locked):** `b45c4b005c9171172d055697a55034006bb0f2fe`
**Latest local repair SHA:** `b79c4f05784d001268e3fef523755365b1f5888e`
**Wheel:** `capt_solo-0.5.0-py3-none-any.whl`
**Wheel SHA-256:** `348fe9da477e0323d9c9b294677a1e10de4f9245373a27300367a9e8bdf879b3`

**Current release state:** `CAPT_MODEL_OPERATOR_PROVEN_AND_RELEASE_READY`
- Verdict issued for `b45c4b0`.
- Verification-contract repair committed at `b79c4f0` **after** the verdict — supersession recorded below.

**Audit scope:** final functionality, public claim, treasure-chest audit, verification-contract defect discovery and repair, installed lifecycle proof.

**Terminal verdict status:**
- `ISSUED` at `b45c4b0`.
- `SUPERSEDED` by repair commit `b79c4f0`: verification contract now conforms; full suite green at **766 passed, 12 deselected, exit 0**.

**Known limitations:**
- Model backend is **external** (Hermes CLI); no in-process model driver is packaged.
- No packaged **OpenAI-compatible driver** — the public API contract assumes a local Hermes installation.
- Commits are **local-only** (not pushed to remote) as of audit date.
- Verification-contract `_view` mechanism needs a **future separation refactor** to decouple view-state from contract-state cleanly.

---

## Evidence Categorization

### VERIFIED FACT

Each item below was independently confirmed from evidence files (git state, test output, wheel hash, artifact inspection) read during this audit session — not merely reported.

- **Source SHA `b45c4b0`** — confirmed as the release-locked candidate via `git rev-parse` / `git log` in the worktree.
- **Branch `release/capt-standalone-final`** — confirmed present and checked out.
- **Worktree clean** — `git status` reported no uncommitted / untracked changes at the audit checkpoint (after `b79c4f0` repair was committed).
- **Full suite green** — `766 passed, 12 deselected, exit 0` reproduced from the local test run against the post-repair tree.
- **Frozen contracts unchanged** — the frozen contract set (manifest, schema, registry documents) byte-for-byte unchanged between `b45c4b0` and `b79c4f0`; the repair commit touched only the verification-contract `_view` mechanism, not frozen contracts.
- **Wheel SHA-256 `348fe9da…`** — `sha256sum` on the installed wheel matches the recorded digest `348fe9da477e0323d9c9b294677a1e10de4f9245373a27300367a9e8bdf879b3`.
- **3 real model artifacts produced through the installed wheel** — end-to-end claim/task/session artifacts generated by invoking the **installed** `capt_solo` package (not the source tree), each artifact's provenance and signature verified.
- **7/7 adversarial authority cases rejected** — seven forged / replayed / mismatched-authority claim submissions were all rejected by the authenticated harness gate; zero false accepts.
- **Idempotency conflict rejected** — a duplicate-checkpoint (same claim, same epoch) replay was detected and rejected; no duplicate state mutation occurred.
- **Checkpoint / restart / resume / no-repeat proven** — full lifecycle: checkpoint persisted, process restarted from cold, resume loaded checkpoint, no-repeat guard prevented replayed task execution, terminal state consistent.

### REPORTED BUT NOT INDEPENDENTLY VERIFIED

- **Prior session's terminal verdict** (`CAPT_MODEL_OPERATOR_PROVEN_AND_RELEASE_READY` issued for `b45c4b0`) — the verdict was issued **before** the verification-contract `_view` defect was discovered and repaired at `b79c4f0`. The verdict text was read in-session logs, but the verdict's own pre-repair test run could not be re-executed against the then-current (defective) tree state. The post-repair run supersedes it.
- **Remote push status** — no `git push` to a remote was attempted this session. Local commits exist at `b45c4b0` and `b79c4f0`; remote sync is unconfirmed.

### INFERENCE

- Because the frozen contract set is byte-identical between `b45c4b0` and `b79c4f0`, and the only repair delta is the `_view` mechanism (test-facing, not contract-facing), the release-locked candidate's **contractual** validity at `b45c4b0` is inferred to hold — the defect was in the **verification harness's view** of the contracts, not in the contracts themselves.
- Because the full suite is green at `b79c4f0` and the repair is narrowly scoped, the `b45c4b0` verdict's *functional* claims (model artifacts, lifecycle, adversarial rejection) are inferred to remain valid; only the *verification-contract conformance* aspect required the supersession.

### DEFERRED

Items explicitly out of scope for v0.5 release; tracked for v0.6 or later.

- **ECP v0.6** — Enhanced Cognitive Protocol v0.6 roadmap item.
- **Plugin version axis** — multi-version plugin compatibility / co-installation matrix.
- **OpenAI-compatible driver** — a packaged in-process model driver implementing the OpenAI-compatible API surface, removing the external-Hermes-CLI dependency.
- **Multi-user identity** — multi-operator / multi-tenant identity & authority scoping.
- **ClaimGuard registry** — persistent, queryable ClaimGuard registry beyond the in-process store.
- **`_view` separation refactor** — decouple verification-contract view-state from contract-state per the defect note; the `b79c4f0` repair is a conformance patch, not the architectural separation.

### MISSING EVIDENCE

- **Remote push confirmation** — no `git push` executed; remote branch state unknown.
- **CI / PR URL** — no continuous-integration run or pull-request link captured; the suite was run locally only.
- **Wheel rebuilt after `b79c4f0` repair** — the audited wheel (`348fe9da…`) was built from the pre-repair tree. A wheel rebuilt from `b79c4f0` has not been produced or hashed. If the repair changes test-facing code that ships in the wheel, a rebuild + re-hash is required before a post-repair release artifact can be trusted.
- **Installed end-to-end claim lifecycle after verification-contract repair** — the `3 real model artifacts` result was produced against the **pre-repair** installed wheel. Re-confirming the full installed-wheel lifecycle against a post-repair rebuild is an open evidence gap.

---

## Document Map

All audit-package files reside in this vault folder unless otherwise stated. Files marked **NOT YET GENERATED** are planned members of the package; this index is written first and siblings follow.

| # | File | Status |
|---|------|--------|
| 00 | `[[00 — CAPT Standalone Harness v0.5 Audit Index]]` | ✅ Generated (this document) |
| 01 | `[[01 — Source Tree & Git State Snapshot]]` | NOT YET GENERATED |
| 02 | `[[02 — Wheel Build & Hash Verification]]` | NOT YET GENERATED |
| 03 | `[[03 — Full Suite Test Run — 766 Passed / 12 Deselected / Exit 0]]` | NOT YET GENERATED |
| 04 | `[[04 — Frozen Contracts Unchanged Proof]]` | NOT YET GENERATED |
| 05 | `[[05 — Verification-Contract _view Defect — Discovery & Root Cause]]` | NOT YET GENERATED |
| 06 | `[[06 — Repair Commit b79c4f0 — Diff & Conformance Report]]` | NOT YET GENERATED |
| 07 | `[[07 — Installed-Wheel End-to-End Lifecycle — 3 Model Artifacts]]` | NOT YET GENERATED |
| 08 | `[[08 — Adversarial Authority Cases — 7/7 Rejected]]` | NOT YET GENERATED |
| 09 | `[[09 — Idempotency & No-Repeat Guard Proofs]]` | NOT YET GENERATED |
| 10 | `[[10 — Checkpoint / Restart / Resume Lifecycle Proof]]` | NOT YET GENERATED |
| 11 | `[[11 — Terminal Verdict & Supersession Record]]` | NOT YET GENERATED |
| 12 | `[[12 — Deferred Items & v0.6 Roadmap]]` | NOT YET GENERATED |

---

## Related Vault Links

- `[[CAPT]]` — top-level CAPT knowledge-base entry (parent area).
- `[[Assessments & Reviews]]` — sibling review indexes for other CAPT releases.

---

> **Audit provenance:** This index was generated from evidence files read during a live audit session on 2026-08-05 against the local worktree `/tmp/capt-final-integration` on branch `release/capt-standalone-final`. Every fact under **VERIFIED FACT** is backed by a sibling document (01–12) or by direct tool output captured this session. Items under **REPORTED BUT NOT INDEPENDENTLY VERIFIED**, **DEFERRED**, or **MISSING EVIDENCE** are flagged so downstream consumers do not treat them as established.
