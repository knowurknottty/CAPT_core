# CURRENT_STATE.md — Authoritative Live State

> Machine-derived portions are regenerated by `capt workspace checkpoint` /
> `capt workspace status`. Snapshot captured at RC convergence session start
> (HEAD `03df3bc`). Verify live with `capt workspace validate`.

- **branch**: `integration/full-public-architecture`
- **head**: `03df3bc` (full: see `git rev-parse HEAD`)
- **cleanliness**: clean working tree at session start (no staged/unstaged tracked modifications before this session's edits).
- **current release target**: `CAPT_core` public release, version `0.4.1` (pyproject + plugin.json). README, release docs, and installer banners reconciled to `0.4.1` (prior drift `v0.1`/`v0.4.0` repaired in stewardship session).
- **completed phases**: 3A–3M (registry/fitness through release verification) + stewardship Universal Workspace layer (TASK-100..207).
- **active phase**: RC convergence — release audit, canon-drift repair, governance classification, workspace maturity, security pass, DX.
- **known failing tests**: none. `python3 -m pytest -q` → **497 passed** (verified this session).
- **known architectural debt**: `architecture/debt.yaml` reconciled to `resolved` for all 17 items (evidence-backed; addressed across Phases 3B–3M). No open debt items remain in the machine-readable tracker.
- **unresolved owner decisions** (require owner, not steward):
  - [B] public/private boundary for research_package modules (FILT/FSR/NEDA/CONS/QIPC/OUROBOROS) — registry classifies them `research_package`; none are present in the `capt-solo` tree (all `implementation_status: missing`, pointing to `biocapt-ecosystem`). Classification prepared for owner review in `docs/RELEASE_GOVERNANCE.md`.
  - [S] privacy review for Consent/Sync transports at integration.
  - [B]+[S] for PULSE/RYS network gateways (registry: `optional_plugin`/`external_package`; safe abstract contracts only; not in core).
- **current public-release blockers (evidence-backed)**:
  1. LICENSE file — **resolved** (MIT LICENSE created; consistent with pyproject declaration).
  2. Version identity drift — **resolved** (README/pyproject/plugin/release-docs/installer banners all `0.4.1`).
  3. Stale release docs (355/45 vs 463/46) — **resolved** (reconciled; live = 497/46).
  4. I-15 missing from CAPT_CANON table — **resolved** (added; ADR-0006 established it).
  5. Universal Workspace layer absent — **resolved** (root AGENTS.md/WORKSPACE.md/etc. + `capt workspace` CLI).
- **remaining release gate**: owner [B]/[S] boundary decisions (governance review prepared, not autonomously decided).
- **last verified full test result**: `497 passed` (this session, `python3 -m pytest -q`).
- **last verified runtime result**: `46 pass / 0 warn / 0 fail / 0 skip` (this session, `python3 verify_runtime.py`).
- **last verified registry result**: `15 checks, 0 fail, 0 warn` (this session, `python3 architecture/validate_registry.py`).
- **generated_at**: 2026-07-27 (RC convergence session)
- **source_commit**: `03df3bc`
