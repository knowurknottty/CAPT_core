# CURRENT_STATE.md — Authoritative Live State

> Machine-derived portions are regenerated by `capt workspace checkpoint` /
> `capt workspace status`. Snapshot at engine convergence session
> (HEAD `27ce5fc`). Verify live with `capt workspace validate`.

- **branch**: `integration/full-public-architecture`
- **head**: `27ce5fc` (full: see `git rev-parse HEAD`)
- **cleanliness**: working tree has uncommitted engine work (M2 math) pending commit at last check; now committed as part of M2 milestone.
- **current release target**: `CAPT_core` public release, version `0.4.1`, MIT (Decision 6). **Not published** (Decision 5).
- **owner decisions applied**: D1 research-public-if-real (none in tree → specs); D2 memory finished+public (HMC/ENGRAM/DREAM stay CAPT_core, reconciled to partial); D3 Puter/mesh private; D4 PULSE public/RYS private; D5 no publish; D6 MIT. See ADR-0007 + docs/RELEASE_GOVERNANCE.md.
- **completed phases**: RC convergence (497→502) + engine convergence M1 (governance) + M2 (mathematics engine built & tested).
- **active phase**: M3 Physics engine (on math substrate).
- **known failing tests**: none. `python3 -m pytest -q` → **550 passed** (verified this session).
- **known architectural debt**: debt.yaml resolved (17 items). New: physics/invention/memory-convergence pending build.
- **current public-release blockers (evidence-backed)**:
  1. Mathematics engine — **built & tested** (M2).
  2. Physics engine — **in progress** (M3).
  3. Invention engine — pending (M4).
  4. Memory convergence — pending (M5).
  5. PULSE hardening / boundary enforcement — pending (M6).
- **remaining release gate**: owner publication (D5). No code-level blocker.
- **last verified full test result**: `550 passed` (this session).
- **last verified runtime result**: `46 pass / 0 warn / 0 fail / 0 skip` (prior session; re-run pending at M8).
- **last verified registry result**: `15 checks, 0 fail, 0 warn` (this session).
- **generated_at**: 2026-07-27 (engine convergence session)
- **source_commit**: `27ce5fc`
